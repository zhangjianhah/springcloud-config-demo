server:
  port: 9001
spring:
  application:
    name: service-product
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop?serverTimezone=UTC&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root.1217
  jpa:
    database: MySQL
    show-sql: true
    open-in-view: true
  rabbitmq:
    host: 127.0.0.1
    username: guest
    password: guest
    port: 15762
#配置eureka client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/
  instance:
    prefer-ip-address: true #使用ip注册地址


name:
  testDir
